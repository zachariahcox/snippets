# SLSA Go builder config - linux/arm64
version: 1
env:
  - GO111MODULE=on
  - CGO_ENABLED=0
flags:
  - -trimpath
  - -tags=netgo
goos: linux
goarch: arm64
binary: snippets-linux-arm64
ldflags:
  - -s -w -X main.Version=$(VERSION) -X main.BuildDate=$(BUILD_DATE)
