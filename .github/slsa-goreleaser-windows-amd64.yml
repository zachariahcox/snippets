# SLSA Go builder config - windows/amd64
version: 1
env:
  - GO111MODULE=on
  - CGO_ENABLED=0
flags:
  - -trimpath
  - -tags=netgo
goos: windows
goarch: amd64
binary: snippets-windows-amd64.exe
ldflags:
  - -s -w -X main.Version=$(VERSION) -X main.BuildDate=$(BUILD_DATE)
