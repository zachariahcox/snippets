# SLSA Go builder config - darwin/arm64
version: 1
env:
  - GO111MODULE=on
  - CGO_ENABLED=0
flags:
  - -trimpath
  - -tags=netgo
goos: darwin
goarch: arm64
binary: snippets-darwin-arm64
ldflags:
  - -s -w -X main.Version=$(VERSION) -X main.BuildDate=$(BUILD_DATE)
